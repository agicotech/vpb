`screen -dmS install bash -c 'curl https://raw.githubusercontent.com/agicotech/vpb/main/setup.sh | sh | tee inst.log'`

`screen -r install`
`Ctrl + D Для выхода`

`screen -dmS install bash -c 'curl https://raw.githubusercontent.com/agicotech/vpb/main/setup.sh proxy| sh | tee inst.log'`
